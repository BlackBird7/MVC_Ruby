<%= form_with(model: registro_orden, local: true) do |form| %>
  <% if registro_orden.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registro_orden.errors.count, "error") %> prohibited this registro_orden from being saved:</h2>

      <ul>
      <% registro_orden.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :numero_orden %>
    <%= form.number_field :numero_orden %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
